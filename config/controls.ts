import type { ReplayableControlsInput } from '@replayablejs/config';

export default {
  // Network profiles resolve whether this gameplay CTA is permitted.
  persistentCta: true,
} satisfies ReplayableControlsInput;
